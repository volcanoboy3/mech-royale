# iOS App

This folder is reserved for the native Capacitor/Xcode project. The project is
generated from the root game after the organized web version is backed up.

The iPhone app will package the game locally so matches can run without loading
the Vercel website inside a browser. Xcode is required to run the simulator,
sign the app, archive it, and upload it to App Store Connect.
